import { CaptureComponentOutletDirective } from './capture-component-outlet.directive';

describe('CaptureComponentOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptureComponentOutletDirective();
    expect(directive).toBeTruthy();
  });
});
